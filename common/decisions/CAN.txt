CAN_young_economists = {
CAN_eco1 = {
    id = generic_industry 
    available = {
        has_stability < 1
    }
    visible = {
        always = yes 
    }
    cost = 50 
    fire_only_once = yes 
    complete_effect = {
        add_stability = 0.1
    }
}
CAN_house_of_commons_36 = {
    available = { date > 1937.6.11}
    activation = {has_completed_focus = CAN_rowell_sirois_commission}
        days_mission_timeout = 365 
        cancel_trigger = {
            has_war = yes
        }
        icon = GFX_decision_mission_icon
            selectable_mission = no 
            if = { var_unemployment < 20
            timeout_effect = {
                add_ideas = CAN_House_sucess_36 
            }
            }
            if { var_unemployment > 20 
            timeout_effect = {
                remove_country_leader 
            }
            }
}

CAN_house_of_commons_37 = {
    available = { date > 1940.6.11}
    activation = {date > 1937.1.1}
        days_mission_timeout = 365 
        cancel_trigger = {
            has_war = yes
        }
        icon = GFX_decision_mission_icon
            selectable_mission = no 
            if = { var_unemployment < 20
            timeout_effect = {
                add_ideas = CAN_House_sucess_37
            }
            }
            if { var_unemployment > 20 
            timeout_effect = {
                remove_country_leader 
            }
            }
}

CAN_house_of_commons_38 = {
    available = { date > 1940.6.11}
    activation = {date > 1938.1.1}
        days_mission_timeout = 365 
        cancel_trigger = {
            has_war = yes
        }
        icon = GFX_decision_mission_icon
            selectable_mission = no 
            if = { var_unemployment < 20
            timeout_effect = {
                add_ideas = CAN_House_sucess_38
            }
            }
            if { var_unemployment > 20 
            timeout_effect = {
                remove_country_leader 
            }
            }
}

CAN_house_of_commons_39 = {
    available = { date > 1940.6.11}
    activation = {date > 1939.1.1}
        days_mission_timeout = 365 
        cancel_trigger = {
            has_war = yes
        }
        icon = GFX_decision_mission_icon
            selectable_mission = no 
            if = { var_unemployment < 20
            timeout_effect = {
                add_ideas = CAN_House_sucess_39
            }
            }
            if { var_unemployment > 20 
            timeout_effect = {
                remove_country_leader 
            }
            }
}
CAN_empleado = {
    id = generic_industry 
    available = {
        
    }
    visible = {
        always = yes 
    }
    cost = 1
    fire_only_once = yes 
    complete_effect = {
    add_to_variable = { unemployment = 10 }
    }
}


}






















































































































































CAN.var_unemployment = {
}

}
