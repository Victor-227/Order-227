
SPR_the_inevitable_civil_war = {

    SPR_military_plot_nationalists = {

		priority = 10

		icon = generic_ignite_civil_war

		allowed = {
            original_tag = SPR
        }

        visible = {
			tag = SPR
		}

        available = {
            always = no
        }

        visible_when_empty = yes
		selectable_mission = yes
		fire_only_once = yes
		cancel_if_not_visible = no
		is_good = yes

        days_mission_timeout = 250

        complete_effect = {
		    NOT = { has_global_flag = spanish_civil_war }
		}

		timeout_effect = {
			country_event = lar_spain.2
			if = {
				limit = {
					has_idea = SPA_awaiting_the_rebellion
				}
				remove_ideas = SPA_awaiting_the_rebellion
			}
		}
	}

    SPR_primo_de_rivera_speech = {

		icon = eng_propaganda_campaigns

		allowed = {
			tag = SPR
		}

		available = {
	        always = yes
		}

		cost = 30

		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		days_remove = 37

		modifier = {
			
		}

		visible = {
            always = yes 
		}

		complete_effect = {
			set_country_flag = SPR_inevitable_civil_war_in_progress_flag
		}

		remove_effect = {
			add_popularity = {
			    ideology = fascism
			    popularity = 0.05
			}
            add_days_mission_timeout = {
				mission = SPR_military_plot_nationalists
				days = -36
			}
			add_days_mission_timeout = {
				mission = SPA_military_plot_republicans
				days = -36
			}
			clr_country_flag = SPR_inevitable_civil_war_in_progress_flag
			if = {
				limit = {
					NOT = { has_country_flag = SPR_primo_de_rivera_first_speech }
				}
				set_country_flag = SPR_primo_de_rivera_first_speech
			}
		}

	}

    SPR_suppress_the_strikes = {

		icon = oppression

		allowed = {
			tag = SPR
		}

		available = {
			NOT = {
				has_country_flag = SPR_inevitable_civil_war_in_progress_flag
			}
			NOT = {
				custom_trigger_tooltip = {
					tooltip = SPA_suppress_the_strikes_tt
					has_idea = SPR_national_strikes_1
				}
			}
		}

		cost = 30

		fire_only_once = no

		ai_will_do = {
			base = 200
		}

		days_remove = 37

		modifier = {
			
		}

		visible = {
			has_country_flag = SPR_spanish_elections_flag
			has_completed_focus = SPA_a_great_spain
			NOT = { has_global_flag = spanish_civil_war }
		}

		complete_effect = {
			set_country_flag = SPR_inevitable_civil_war_in_progress_flag
		}

		remove_effect = {
            add_days_mission_timeout = {
				mission = SPR_military_plot_nationalists
				days = -36
			}
			add_days_mission_timeout = {
				mission = SPA_military_plot_republicans
				days = -36
			}
			SPR_suppress_the_strikes_increase = yes
			clr_country_flag = SPR_inevitable_civil_war_in_progress_flag
		}

	}

}
