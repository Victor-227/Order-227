GER_oberkommando_der_wehrmacht_dec_cat = {
    GER_fall_weiss = {
        icon = GFX_decision_ger_reichskommissariats

        allowed = {
            tag = GER
        }

        available = {
            has_completed_focus = GER_danzig_or_war
            date > 1939.9.1
        }

        visible = {
		    always = yes
	    }

        fire_only_once = yes

        complete_effect = {
            GER = {
                country_event = { id = germany.88}
            }
        }
    }

    GER_unternehmen_weserubung = {
        icon = GFX_decision_ger_reichskommissariats

        allowed = {
            tag = GER
        }

        available = {
            has_completed_focus = GER_operation_weserubung
        }

        visible = {
		    always = yes
	    }
        
        fire_only_once = yes

        complete_effect = {
            declare_war_on = {
			    type = annex_everything
			    target = DEN
		    }
            declare_war_on = {
			    type = annex_everything
			    target = NOR
		    }
            hidden_effect = {
                ENG = { add_to_faction = DEN }
                ENG = { add_to_faction = NOR }
                every_other_country = {
                    limit = {
                        NOT = {
                            tag = ITA
                        }
                        is_in_faction_with = GER
                    }
                    add_to_war = { 
						targeted_alliance = GER
						enemy = DEN
					}
                    add_to_war = { 
						targeted_alliance = GER
						enemy = NOR
					}      
                }
            }
        }
    }

    GER_fall_gelb = {
        icon = GFX_decision_ger_reichskommissariats

        allowed = {
            tag = GER
        }

        available = {
            has_completed_focus = GER_around_maginot
        }

        visible = {
		    always = yes
	    }
        
        fire_only_once = yes

        complete_effect = {
            declare_war_on = {
			    type = annex_everything
			    target = BEL
		    }
            declare_war_on = {
			    type = annex_everything
			    target = HOL
		    }
            custom_effect_tooltip = GER_fall_gelb_tt
            hidden_effect = {
                ENG = { add_to_faction = BEL }
                ENG = { add_to_faction = HOL }
                every_other_country = {
                    limit = {
                        is_in_faction_with = GER
                    }
                    add_to_war = { 
						targeted_alliance = GER
						enemy = BEL
					}
                    add_to_war = { 
						targeted_alliance = GER
						enemy = HOL
					}      
                }
            }
        }
    }

    GER_unternehmen_marita = {
        icon = GFX_decision_ger_reichskommissariats

        allowed = {
            tag = GER
        }

        available = {
            has_completed_focus = GER_fate_of_greece
        }

        visible = {
		    always = yes
	    }
        
        fire_only_once = yes

        complete_effect = {
            declare_war_on = {
			    type = annex_everything
			    target = GRE
		    }
            hidden_effect = {
                ENG = { add_to_faction = GRE }
                every_other_country = {
                    limit = {
                        is_in_faction_with = GER
                    }
                    add_to_war = { 
						targeted_alliance = GER
						enemy = GRE
					}   
                }
            }
        }
    }

    GER_unternehmen_25 = {
        icon = GFX_decision_ger_reichskommissariats

        allowed = {
            tag = GER
        }

        available = {
            has_completed_focus = GER_fate_of_yugoslavia
        }

        visible = {
		    always = yes
	    }
        
        fire_only_once = yes

        complete_effect = {
            declare_war_on = {
			    type = annex_everything
			    target = YUG
		    }
            hidden_effect = {
                ENG = { add_to_faction = YUG }
                every_other_country = {
                    limit = {
                        is_in_faction_with = GER
                    }
                    add_to_war = { 
						targeted_alliance = GER
						enemy = YUG
					}   
                }
            }
        }
    }

    GER_unternehmen_barbarossa = {
        icon = GFX_decision_ger_reichskommissariats

        allowed = {
            tag = GER
        }

        available = {
            has_completed_focus = GER_war_with_the_ussr
        }

        visible = {
		    always = yes
	    }
        
        fire_only_once = yes

        complete_effect = {
            declare_war_on = {
			    type = annex_everything
			    target = SOV
		    }
            hidden_effect = {
                every_other_country = {
                    limit = {
                        is_in_faction_with = GER
                    }
                    add_to_war = { 
						targeted_alliance = GER
						enemy = SOV
					}   
                }
            } 
        }
    }
}