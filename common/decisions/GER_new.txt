GER_oberkommando_der_wehrmacht_dec_cat = {
    GER_fall_weiss = {
        icon = GFX_decision_ger_reichskommissariats

        allowed = {
            tag = GER
        }

        available = {
            has_completed_focus = GER_danzig_or_war
        }

        visible = {
		    always = yes
	    }

        fire_only_once = yes

        complete_effect = {
            every_country = {
                limit = {
                    is_in_faction_with = GER
                }
                declare_war_on = {
                    target = POL
                    type = annex_everything
                }
            }
        }
    }

    GER_unternehmen_weserübung = {
        icon = GFX_decision_ger_reichskommissariats

        allowed = {
            tag = GER
        }

        available = {
            has_completed_focus = GER_danzig_or_war
        }

        visible = {
		    always = yes
	    }
        
        fire_only_once = yes

        complete_effect = {
            every_country = {
                limit = {
                    is_in_faction_with = GER
                }
                declare_war_on = {
                    target = POL
                    type = annex_everything
                }
            }
        }
    }

    GER_fall_gelb = {
        icon = GFX_decision_ger_reichskommissariats

        allowed = {
            tag = GER
        }

        available = {
            has_completed_focus = GER_danzig_or_war
        }

        visible = {
		    always = yes
	    }
        
        fire_only_once = yes

        complete_effect = {
            every_country = {
                limit = {
                    is_in_faction_with = GER
                }
                declare_war_on = {
                    target = POL
                    type = annex_everything
                }
            }
        }
    }

    GER_unternehmen_Marita = {
        icon = GFX_decision_ger_reichskommissariats

        allowed = {
            tag = GER
        }

        available = {
            has_completed_focus = GER_danzig_or_war
        }

        visible = {
		    always = yes
	    }
        
        fire_only_once = yes

        complete_effect = {
            every_country = {
                limit = {
                    is_in_faction_with = GER
                }
                declare_war_on = {
                    target = POL
                    type = annex_everything
                }
            }
        }
    }

    GER_unternehmen_25 = {
        icon = GFX_decision_ger_reichskommissariats

        allowed = {
            tag = GER
        }

        available = {
            has_completed_focus = GER_danzig_or_war
        }

        visible = {
		    always = yes
	    }
        
        fire_only_once = yes

        complete_effect = {
            every_country = {
                limit = {
                    is_in_faction_with = GER
                }
                declare_war_on = {
                    target = POL
                    type = annex_everything
                }
            }
        }
    }

    GER_unternehmen_barbarossa = {
        icon = GFX_decision_ger_reichskommissariats

        allowed = {
            tag = GER
        }

        available = {
            has_completed_focus = GER_danzig_or_war
        }

        visible = {
		    always = yes
	    }
        
        fire_only_once = yes

        complete_effect = {
            every_country = {
                limit = {
                    is_in_faction_with = GER
                }
                declare_war_on = {
                    target = POL
                    type = annex_everything
                }
            }
        }
    }
}